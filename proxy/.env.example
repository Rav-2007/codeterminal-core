# Local dev only. Real values go in .env (gitignored, see repo root .gitignore)
# or, in production, a Railway environment variable/secret -- never a file.
OPENROUTER_API_KEY=
OPENROUTER_API_BASE=https://openrouter.ai/api/v1
PORT=8080
